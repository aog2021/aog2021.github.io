function drop(e) {
    e.preventDefault();
    console.log(e)
}

function allowDrop(e) {
    e.preventDefault();
    console.log(e)
}

function handleFiles(e){
    ev.preventDefault();
    console.log(e.target.value)
}